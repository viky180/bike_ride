# Supabase Configuration
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Firebase Configuration (for Phone Authentication)
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=

# Admin Configuration
# NOTE: Admin access is now managed via the `is_admin` column in the users table.
# To grant admin access, run in Supabase SQL Editor:
# UPDATE users SET is_admin = true WHERE phone = 'YOUR_PHONE_NUMBER';
